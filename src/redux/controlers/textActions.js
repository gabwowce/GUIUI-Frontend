export const setText = (text) => ({
    type: 'SET_BUTTON_TEXT',
    payload: text,
  });
  
  export const setTextFont = (font) => ({
    type: 'SET_TEXT_FONT',
    payload: font,
  });
  
  export const setTextColor = (color) => ({
    type: 'SET_TEXT_COLOR',
    payload: color,
  });
  
  export const setFontSize = (size) => ({
    type: 'SET_FONT_SIZE',
    payload: size,
  });
  
  export const setFontWeight = (weight) => ({
    type: 'SET_FONT_WEIGHT',
    payload: weight,
  });
  
  export const setUppercase = (value) => ({
    type: 'SET_UPPERCASE',
    payload: value,
  });
  
  export const setLetterSpacing = (spacing) => ({
    type: 'SET_LETTER_SPACING',
    payload: spacing,
  });
  
  export const setWordSpacing = (spacing) => ({
    type: 'SET_WORD_SPACING',
    payload: spacing,
  });
  
  export const setTextGradient = (gradient) => ({
    type: 'SET_TEXT_GRADIENT',
    payload: gradient,
  });
  